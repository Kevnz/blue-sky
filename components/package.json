{
  "name": "components",
  "version": "1.0.0",
  "description": "I have been working on a talk on learning web components. However the landscape is sad. Vanilla Web Components are very verbose, especially when doing DOM work. And then the fact the js is class based too.",
  "main": "process.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-html-parser": "^1.1.15",
    "parse5": "^5.1.0"
  }
}
